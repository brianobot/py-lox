Draw Backs of JIT

- Requires taming the dynamic type systems
- Memory Overhead, the original source code and the JITed version needs to be stored
- Some events can invalidate the code for JIT requiring a time consuming recompilation
- Security Issues
- Unlike a pure interpreted language, JIT is less portable since the compiled part must target a specific architecture
