
Some Sample Lox Code

```lox
var first_name = "Brian";
var last_name = "Obot";

var fullname = first_name + last_name;

print fullname;


// I could not run the Multi-Line Program in the REPL version of the clox interpreter
class Car {
  init(year, color, model) {
    this.year = year;
    this.color = color;
    this.model = model;
  }

  model() {
    print this.model;
  }
}
```
